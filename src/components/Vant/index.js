import Vue from 'vue'

import {
    Field,
    Tabbar,
    TabbarItem,
    Uploader,
	Toast,
	NavBar
} from 'vant'
Vue.use(Field)
    .use(Tabbar)
    .use(TabbarItem)
    .use(Uploader)
    .use(NavBar)
	.use(Toast)